apiVersion: apps/v1
kind: Deployment
metadata:
  name: litellm-redis
  namespace: kserve-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: litellm-redis
  template:
    metadata:
      labels:
        app: litellm-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: litellm-redis
  namespace: kserve-test
spec:
  selector:
    app: litellm-redis
  ports:
  - port: 6379
    targetPort: 6379
